[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "clipador-clip-editor"
version = "0.1.0"
description = "Wrapper para integração do módulo Clip Editor no monorepo"
requires-python = ">=3.11"

[tool.hatch.build]
packages = ["src/clipador_clip_editor"]
